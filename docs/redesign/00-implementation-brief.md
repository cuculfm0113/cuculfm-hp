# CUCUL FM サイト改修 実装ブリーフ（FDE・AI実装支援 追加）

このドキュメントは、Cursor（Claude Fable 5）で行った要件整理・コードベース調査・計画策定の成果を、実装担当AI（Claude Code / Opus 5 等）へ引き継ぐための**完全な指示書**である。実装前に必ず全文を読むこと。

- 要件の原文は本書「8. 元の要件定義（全文）」に収録している。コピー（文言）はそこから**一字一句そのまま**使うこと
- 実装は「4. フェーズ分割と実行手順」の順に進め、フェーズごとに git commit する
- 本書と要件原文が矛盾する場合は、本書「1. 確定事項」が優先（発注者と合意済みのため）

---

## 1. 前提と確定事項（発注者と合意済み）

1. **静的HTML構成を維持して拡張する**。Next.js等へのリビルドはしない。ビルド必須化もしない（後述のスクリプトは任意実行）
2. **トップページの既存演出はすべて残す**（3Dキャラ、GSAPスクロールシーケンス、掲示板、About、星空/月テーマ）。同じ世界観で新セクションを統合する
3. お問い合わせフォームは mailto方式 → **Netlify Forms** へ移行（ホスティングがNetlifyのため）
4. 記事スラッグ `/insights/sas-vs-custom` は **`/insights/saas-vs-custom`** に修正して実装（SaaSの表記に合わせる）
5. 表記統一は既存サイトに合わせ **「CUCUL FM.LLC」（スペースなし）/ ククルFM合同会社 / ククルFM** とする（要件原文の「CUCUL FM .LLC」は採用しない）
6. デプロイは `git add . && git commit && git push` 後、まず `netlify deploy --dir=.`（プレビュー）→ 発注者確認後に `netlify deploy --prod --dir=.`。**本番反映は発注者の確認を得てから**
7. 特定商取引法ページは今回作らない（EC直販が現状ないため。チェックリストに将来項目として記載のみ）
8. スパム対策は初期は honeypot のみ（reCAPTCHAはキー取得が必要なため将来オプションとしてドキュメント化）
9. 法人番号・設立年月日など未確定情報は**推測で埋めず**、`content/site.config.json` の空欄プレースホルダーとし、空欄時はHTML/JSON-LDに出力しない
10. **公開用メールアドレスは `info@cucul-fm.com`**（2026-08-29 発注者指示により `cuculinfo0113@gmail.com` から変更）。サイト表示・JSON-LD・Netlify Forms の通知先すべてこれを使う。本項は以降の全フェーズで優先する
    - ただし `/contact/form-handler.js` の mailto 宛先は**本番稼働中のためフェーズ1では変更しない**（2026-08-29 発注者判断）。**フェーズ4の Netlify Forms 化と同時に切り替える**。フェーズ4では form-handler.js を書き換えるため mailto 宛先の設定自体が無くなり、通知先は Netlify 管理画面（`content/site.config.json` の `contact.notifyEmail`）に一本化される
    - フェーズ4の着手前に、`info@cucul-fm.com` が受信できる状態かを必ず確認すること

---

## 2. コードベース調査サマリー（2026-08-28時点の事実）

### 技術構成

- 静的HTMLサイト。フレームワーク・ビルドツールなし。ルートに `package.json` なし
- トップ `index.html` は約4,058行の自己完結ページ（インラインCSS/JS + GSAP 3.12.5 CDN + three.js r170 ESM + Google Fonts）
- 下層は `/services/style.css` を共有する静的ページ群
- 本番URL: `https://cucul-fm.com/`（Netlify: cuculfm-hp、Site ID `6121d456-5d60-4371-b63c-25372f32737d`）
- Gitリモート: `cuculfm0113/cuculfm-hp`
- `scripts/` にNode資産あり（`scripts/package.json` に puppeteer、`scripts/cardgen/` に画像生成ツール）→ OGP画像生成に流用可

### index.html セクションマップ（編集前の行番号。編集で変動するため目安）

| 行 | 内容 |
|----|------|
| 4-29 | meta / title / OGP / Twitter Card / canonical / favicon(SVG) |
| 32-67 | JSON-LD `@graph`（WebSite + Organization） |
| 89- | `:root` デザイントークン |
| 1585 | `<section id="hero">`（星空・月・3Dキャラ・巨大タイポ。1602にhero-nav、1623-1624にコーナーコピー） |
| 1861 | `<section id="seq">` スクロール連動シーケンス |
| 1894 | `<section id="business">` 7枚の事業カード（biz-scroller内biz-grid） |
| 1975 | `<section id="news">` 掲示板4カード |
| 2024 | `<section id="about">` 会社情報（spec-table: 運営会社/代表者/経歴/事業内容/所在地/連絡先） |
| 2079 | `<section id="contact">` フォーム（`#contactForm`: name/email/subject/message → `/contact/form-handler.js` がmailto送信） |
| 2128 | `<footer class="site-footer">` 3カラムサイトマップ |
| 2195付近 | `#index-overlay` 全リンクINDEXオーバーレイ（idx-group: services/news/company） |

### デザイントークン（index.html `:root` より）

- フォント: `Orbitron`（display）/ `Bebas Neue`（heading）/ `Noto Sans JP`（body）
- 色: `--c-primary:#f5e100`（黄）/ `--c-secondary:#7b61ff` / `--c-accent:#ff6b6b` / `--c-amber:#fac458` / `--c-cyan:#00cce6` / `--c-mint:#00f5d4`
- 背景: `--t-bg-0:#000510` / `--t-bg-1:#0a0a1a` / `--t-bg-2:#0f0f20`、文字 `#ffffff`、補助 `--t-muted:#8a8a9b`
- `data-theme="moon"`（既定）と red テーマの切替機構あり。コンテナ `--container:1440px`（下層は1200px）
- セクション見出しパターン: `<p class="eyebrow">英語ラベル</p><h2>ENGLISH <em>TITLE</em></h2><p class="lead">日本語リード</p>` を踏襲すること

### 下層ページのテンプレートパターン（例: /services/ai/index.html）

- `<header class="service-header">`（「TOP に戻る」+ ロゴ）→ `<section class="service-hero service-hero--starry">`（星空背景 + hero-label/hero-title/hero-description）→ `content-section` 群（section-label / section-title / features-grid 等）
- 新規ページ（/fde/ ほか）はこのパターンと `/services/style.css` をベースに拡張してよい（専用CSSの追加可）

### 既存ページ一覧

- サービス: `/services/{ai, ai-tools, construction, inspection, tools, craft, custom, video, document, web, media, dog, dog/breeds, dog/pet-floor, condo, condo/garage, condo/hideaway, condo/mahjong}/`
- その他: `/articles/`（記事一覧）、`/blog/{inspection,dog}/` に公開記事13本、`/recruit/`、`/event/`、`404.html`、`_redirects`（/top-v2→/ と旧blogの301）
- About / Contact の独立ページは**存在しない**（トップの `#about` `#contact` アンカーのみ。下層19ページのCTAは `index.html#contact` へ深リンクしている → contactセクションのidは変更禁止）

### SEO・フォームの現状

- robots.txt: 整備済み（AIクローラー許可、Sitemap指定）。変更不要
- sitemap.xml: 手書きの静的リスト。新規ページ追加が必要
- トップ: canonical / OGP / Twitter Card / JSON-LD あり。**OGP画像がSVG**（`/logo/cuculfm.svg`）でSNSプレビュー非対応の可能性 → PNG化が必要
- 下層: title / description はあるが canonical / OGP はほぼ未設置
- フォーム: `/contact/form-handler.js` が mailto（宛先 `cuculinfo0113@gmail.com`）。`/contact/send.php` は未使用
- Google Search Console 検証メタタグ設置済み（`4Bl0lbh-c8RHx9Uqb0QgfwSeRcLh8A6mvKFjFsZxSq0`）

### 会社情報（確定値。site.config.json の初期値に使う）

- 会社名: ククルFM合同会社 / CUCUL FM.LLC
- 代表者: 馬場 陽子
- 代表者経歴（#aboutに既載）: プロセス制御機器メーカーで石油・LNG・化学プラント向け海外プロジェクトにアプリケーションエンジニアとして従事。その後コンサルティング会社数社で電力・ガス・素材業界のDX戦略策定、業務改革、新規事業支援などをEnd to Endで支援。現在はククルFM代表の傍らAIテックカンパニーに従事
- 所在地: 〒333-0802 埼玉県川口市戸塚東４丁目３２−５ ITSUZAI community base １号室
- TEL: 090-6262-3842 / メール: info@cucul-fm.com（2026-08-29 変更。旧 cuculinfo0113@gmail.com）
- SNS: Instagram `cuculfm_llc` / X `madarame365`
- ドメイン: cucul-fm.com
- 法人番号・設立年月日: **未確定（空欄プレースホルダー）**
- 会社情報の既存記載箇所: index.html の #about・フッター・INDEXオーバーレイ・JSON-LD、404.html と /articles/ のフッター、`会社内容.md`（経営理念の一次資料。記事執筆時の思想的裏付けに使うこと）

---

## 3. 実装計画（承認済みプラン全文）

### 改修後サイトマップ

```
/                    トップ（拡張）
/fde/                FDE・AI実装支援（新規・最重要）
/insights/           記事一覧（新規）
/insights/fde-toha/ genba-dx/ ai-teichaku/ gyomu-flow/
         saas-vs-custom/ ai-usecases/ data-foundation/ dx-roadmap/
/about/              会社概要（新規）
/privacy/            プライバシーポリシー（新規・フォーム同意の前提）
/services/*          既存20ページ（維持 + canonical/OGP + FDE導線の小修正）
/blog/ /articles/ /recruit/ /event/  維持
```

### アーキテクチャ: データ配列化とノービルド維持の両立

「FAQ・ロードマップ・活用テーマ等のデータ配列化」と「重要本文をHTMLに直接出力」を両立するため、**コンテンツデータ + 同期スクリプト方式**を採用する。

- `content/` … `site.config.json`（会社情報一元管理）、`pillars.json`、`challenges.json`、`roadmap.json`、`usecases.json`、`faq-top.json`、`faq-fde.json`
- `scripts/build-content.mjs` … **依存パッケージなし**のNodeスクリプト。HTML内の `<!-- BEGIN:xxx --> ... <!-- END:xxx -->` マーカー区間に、データから生成したHTMLと FAQPage 等のJSON-LDを同時注入する（画面表示と構造化データの完全一致を機械的に保証）
- `scripts/generate-sitemap.mjs` … 公開HTMLを走査して `sitemap.xml` を自動生成（下書き・非公開ディレクトリ note/ docs/ scripts/ terminals/ 等は除外）
- スクリプトを実行しなくても、コミットされたHTMLがそのまま正となる（ノービルド維持）

### トップページ拡張（index.html）

セクション順: hero → seq → **FDE横断（新）** → business（3本柱化）→ **課題提起（新）** → **ロードマップ（新）** → **活用テーマ（新）** → news → about → **FAQ（新）** → contact（改修）

- **グローバルヘッダー（新規）**: ロゴ + ナビ（Our Service / FDE・AI実装支援 / Construction & Infrastructure / Creative & Tech / Lifestyle / Insights / About Us / Contact）+ 右上「相談する」CTA。ヒーロー通過後にフェードインする固定バーとし、既存演出を邪魔しない。モバイルはハンバーガー（**既存の #index-overlay を開く形で流用してよい**）+「相談する」は常時視認できる位置に
  - リンク先: Our Service→`/#business`、FDE→`/fde/`、Construction→`/#pillar-construction`、Creative→`/#pillar-creative`、Lifestyle→`/#pillar-lifestyle`、Insights→`/insights/`、About Us→`/about/`、Contact→`/#contact`、相談する→`/#contact`
- **ヒーロー**: 既存の「普段と新しい暮らしをサポートする会社」とビジュアルは維持。その下に「現場と事業に、技術を定着させる。」+ 補足コピー + CTA2つ（ククルFMの事業を見る→#business / FDE・AI実装支援を見る→/fde/）をGSAPフェードで追加
- **FDE横断セクション（新設）**: 「FDE & AI IMPLEMENTATION / 技術を、現場で使われる仕組みへ。」+ 本文 + 01 Understand / 02 Build / 03 Embed 3ステップ図解 + CTA「FDE・AI実装支援の詳細を見る」
- **business**: 冒頭に3本柱ブロック（`#pillar-construction` / `#pillar-creative` / `#pillar-lifestyle` アンカー付き。要件の見出し・本文・FDEつながり補足を使用）を追加し、既存7カードスクローラーは「サービス一覧」としてそのまま維持
- **課題提起**: 指定8課題のカード + 締め文
- **ロードマップ**: 4フェーズカード（タイムライン装飾、施策リスト + 成果物チップ。`<details>` でモバイル折りたたみ可・本文はHTML内に必ず存在させる）+ CTA「自社に合わせた進め方を相談する」→#contact
- **活用テーマ**: 「※実績ではなく活用イメージです」を明記した10項目
- **FAQ**: 指定7問を `<details>` アコーディオン + FAQPage JSON-LD（faq-top.json から同時生成）
- **フッター / INDEXオーバーレイ**: FDE・Insights・About・Privacy リンク追加
- **JSON-LD拡張**: Organization強化（確定情報のみ）+ Service 8件（FDE・AI実装支援 / 業務フロー・DX診断 / AI業務活用支援 / 業務システム・Web開発 / データ整理・データ連携支援 / 映像・資料制作 / 建設・インフラ関連事業 / ライフスタイル関連事業）+ WebPage + FAQPage
- **#about の事業内容行**に「FDE・AI実装支援」を追記

### /fde/ ページ（新規・最重要）

`/services/style.css` を継承した専用ページ。構成:

1. H1「現場を知るから、変革を動かせる。」+ 直下にサブコピー（要件原文のとおり。結論・定義を明記）
2. 思想ブロック「AIはAIらしく、人は人らしく。」を大きく目立たせる（要件原文の全文）
3. 立ち位置: 「AIツール販売会社/Web制作会社/受託開発会社/ITコンサルティング会社**ではない**」の対比 + 「現場伴走型のAI・DX実装支援」を前面に（FDEの語は補足説明扱い）
4. 対象業種の広がり（下水道、建設、設備、保守点検、清掃、物流、製造、不動産・施設管理、クリエイティブ事業、地域事業、サービス業。建設・インフラは強みとして明記しつつ限定しない）
5. 3ステップ詳細（Understand / Build / Embed）
6. 12か月ロードマップ（フル版・トップと同一データソース roadmap.json）
7. 活用テーマ（usecases.json）
8. **AI・データの取扱い方針**: AIに入力してよい情報/扱うべきでない情報、人による確認・承認の考え方、顧客データの取扱い、案件ごとのセキュリティ確認姿勢
9. FDE専用FAQ（faq-fde.json。トップ7問を基に必要に応じ拡張）+ FAQPage JSON-LD
10. CTA + 問い合わせ導線、パンくず（表示 + BreadcrumbList）、Service / WebPage JSON-LD、公開日・監修者表記

### /insights/（一覧 + 記事8本）

一覧ページ + 記事: `fde-toha` `genba-dx` `ai-teichaku` `gyomu-flow` `saas-vs-custom` `ai-usecases` `data-foundation` `dx-roadmap`

各記事の品質基準:

- 一般論の再編集ではなく、**ククルFMの実事業（下水道カメラ調査・建設・清掃の現場実務、代表のプラントエンジニア/コンサルでのDX経験）に根ざした判断基準・手順**を書き下ろす。`会社内容.md` と既存 `/blog/` 記事のトーンを参照
- 架空の顧客名・導入社数・削減率などの数値は**一切書かない**。「必ず」「完全」「100%」等の断定表現禁止
- 1本あたり日本語2,000〜3,500字目安。H1直下に結論1〜3文 → 対象読者と課題 → 本論（H2構造）→ ククルFM独自の視点 → FAQ小節 → CTA
- 執筆者（ククルFM編集部 または 代表 馬場 陽子 監修）・公開日・最終更新日を表示
- Article + BreadcrumbList JSON-LD、canonical、OGP、`/fde/`・関連記事・`/#contact` への内部リンク

### /about/ と /privacy/

- /about/: 会社情報（config由来）、ミッション、大切にする考え方、3本柱とFDEの関係、FDE・AI実装支援の体制、対応地域（川口市拠点・関東近郊+リモートで全国等、断定しすぎない表現）、問い合わせ方法。AboutPage / Organization JSON-LD
- /privacy/: 個人情報の取得目的・利用範囲・第三者提供しない旨・GA4/GTM等のアクセス解析利用・問い合わせ窓口。フォーム同意チェックのリンク先

### お問い合わせフォーム改修（Netlify Forms化）

- フィールド: 会社名または屋号 / お名前* / 部署・役職 / メールアドレス* / 電話番号 / 業種 / ご相談の種類（select: 現場業務の改善・AI活用・Web・業務システム・データ・情報整理・資料・映像制作・新規事業・サービス設計・その他）/ ご相談内容* / 個人情報保護方針への同意*（/privacy/ へリンク）
- 静的HTML側: `<form name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field">` + hidden `form-name`。**Netlifyのフォーム検出は静的HTML内のform定義が必須**
- `/contact/form-handler.js` を書き換え: fetch POST（`application/x-www-form-urlencoded`）で送信、必須/メール形式バリデーション（alertではなくインライン表示）、送信中はボタン無効化+「送信中...」、成功/失敗メッセージ表示、dataLayerイベント発火
- 下層19ページからの `index.html#contact` 深リンクが壊れないよう **section id="contact" と form周辺構造の互換性を保つ**

### SEO・構造化データ・技術対応

- 全新規ページ + 既存サービス約20ページに canonical / OGP / Twitter Card を整備
- OGP画像: 1200×630 PNG を新規作成（`/images/ogp/ogp-default.png`。既存 scripts/cardgen のPuppeteer資産を流用 or 単純な生成HTMLをスクリーンショット）。全ページの og:image / twitter:image を差し替え
- favicon PNG（192/512）+ `site.webmanifest` 追加
- パンくず: 新規ページに表示 + BreadcrumbList
- 見出しは各ページH1ひとつ。重要本文はJSなしでもHTMLに存在させる（GSAPアニメは opacity/transform のみ、JS無効時も読める初期状態にする）

### 計測基盤（GTM/GA4対応）

- `js/analytics.js`（新規・全ページ共通読込）: `content/site.config.json` の GTM ID が空ならロードしない。dataLayerイベント:
  - `contact_form_view`（IntersectionObserver）/ `contact_form_start`（最初のinput focus）/ `contact_form_submit` / `contact_form_success`
  - `click_phone` / `click_email` / `click_consultation_cta` / `click_fde_service` / `click_roadmap`
  - `scroll_depth_50` / `scroll_depth_90`
- イベント名と実装箇所を docs/redesign/06-analytics.md に一覧化

### ドキュメント成果物（docs/redesign/）

1. `01-sitemap.md` 改修後サイトマップ
2. `02-wireframe-top.md` トップ詳細ワイヤーフレーム
3. `03-wireframe-fde.md` FDEページ詳細ワイヤーフレーム
4. `04-copy-seo.md` ページ別コピー + SEO title/meta description/OGP一覧
5. `05-design-system.md` デザインシステム（既存トークン + 新コンポーネント規約）
6. `06-analytics.md` 計測イベント設計
7. `07-update-guide.md` 会社情報・実績の更新箇所と手順（config とスクリプトの使い方、Netlify通知設定手順含む）
8. `08-launch-checklist.md` 公開前チェックリスト（要件原文の18項目 + Bing Webmaster Tools/AI Performance・Google Business Profile・月次/四半期運用ルール込み）

---

## 4. フェーズ分割と実行手順

**1フェーズ = Claude Code の1セッション（または1プロンプト）**を推奨。各フェーズの冒頭で本ブリーフを読み直し、完了時に git commit する。コンテキストが長くなったら `/compact` するか新セッションで次フェーズへ。

| フェーズ | 内容 | commit例 |
|---------|------|---------|
| 1 | `content/` データ7ファイル + `scripts/build-content.mjs` + `scripts/generate-sitemap.mjs` 作成。マーカー注入方式が index.html を壊さないことをテスト | `feat: コンテンツデータ基盤と同期スクリプトを追加` |
| 2 | トップ: グローバルヘッダー + ヒーロー追加コピー/CTA | `feat: グローバルヘッダーとヒーローの技術定着コピーを追加` |
| 3 | トップ: FDE横断・3本柱・課題提起・ロードマップ・活用テーマ・FAQの6セクション + フッター/INDEX更新 + JSON-LD拡張 | `feat: トップにFDE横断セクション群を追加` |
| 4 | フォームNetlify Forms化 + `/privacy/` 新設 + `js/analytics.js` | `feat: 問い合わせフォームをNetlify Forms化しプライバシーポリシーを追加` |
| 5 | `/fde/` ページ新設（フル） + `/about/` 新設 | `feat: FDE・AI実装支援ページと会社概要ページを新設` |
| 6 | `/insights/` 一覧 + 記事4本（fde-toha / genba-dx / ai-teichaku / gyomu-flow） | `feat: Insights一覧と記事4本を追加` |
| 7 | 記事残り4本（saas-vs-custom / ai-usecases / data-foundation / dx-roadmap） | `feat: Insights記事4本を追加` |
| 8 | SEOパス: OGP用PNG生成・favicon/manifest・既存サービスページへcanonical/OGP・/services/ai/ と /services/web/ に /fde/ への導線追加・sitemap生成 | `feat: OGP画像とサイト全体のSEOメタを整備` |
| 9 | docs/redesign/ 01〜08 のドキュメント作成 | `docs: 改修ドキュメント一式を追加` |
| 10 | 検証（リンク切れ・JSON-LD構文・モバイル・フォーム動作）→ push → `netlify deploy --dir=.` でプレビューURLを発注者へ報告。**本番デプロイは発注者確認後** | `chore: 公開前検証と修正` |

### フェーズ共通の受け入れ基準

- 既存ページ・既存演出・既存リンク（特に `index.html#contact` への深リンク、`#business` アンカー）を壊していない
- 固定コピーは要件原文と一字一句一致
- 新規コンテンツはJS無効でも読める（初期状態で可視）
- 架空の実績・数値・顧客名・断定表現（必ず/完全/100%）が入っていない
- 表記が CUCUL FM.LLC / ククルFM合同会社 / ククルFM で統一されている
- JSON-LD は画面に表示されている内容とのみ一致している

### 検証方法

```bash
# ローカルプレビュー（プロジェクトルートで）
python3 -m http.server 8000
# → http://localhost:8000 でトップ・/fde/・/insights/ 等を確認

# JSON-LD構文: 各ページの <script type="application/ld+json"> を目視 + JSON.parse で検証
# プレビューデプロイ
netlify deploy --dir=.
```

---

## 5. 制約・禁止事項（要件原文より重要抜粋）

- 「FDE」という言葉を前面に出しすぎない。「現場伴走型のAI・DX実装支援」等の平易な表現を優先
- 仮の実績数値、架空の導入社名、架空の顧客ロゴ、根拠のない削減効果を作らない
- 「必ず効率化できる」「完全自動化」「100%正確なAI」などの断定表現を使わない
- AI検索での表示・引用・上位表示を保証する表現をしない。AI向け隠しテキスト・非表示FAQ・内容と一致しない構造化データを作らない
- キーワードの過剰な反復をしない
- 安易なロボット画像、ネオン調、サイバーパンク調、安っぽいAI生成風人物画像を使わない（既存ヒーロービジュアルを維持するため新規ビジュアルは原則不要）
- 全ページ日本語中心。英語見出し + 日本語本文の既存トーンを踏襲

---

## 6. 発注者の手動作業（実装後に依頼すること）

実装完了報告時に、以下を発注者への依頼事項として明記すること（07-update-guide.md にも記載）:

1. Netlify管理画面 → Forms → Form notifications で通知先メール（info@cucul-fm.com）を設定。あわせて info@cucul-fm.com が受信できる状態か確認する
2. GTMコンテナ / GA4プロパティを作成し、IDを `content/site.config.json` に記入 → `node scripts/build-content.mjs` 実行 → 再デプロイ
3. Bing Webmaster Tools へのサイト登録（Search Console連携インポート可）と AI Performance の確認
4. Google Business Profile の登録・情報整合性確認
5. 法人番号・設立年月日が確定したら site.config.json に記入して同期スクリプト実行
6. プレビューURL確認後、本番デプロイの承認

---

## 7. 参考: 元計画ファイル

Cursorのプランファイル: `~/.cursor/plans/fde・ai実装支援_サイト改修_60d7107f.plan.md`（本ブリーフに全内容を反映済みのため参照は任意）

---

## 8. 元の要件定義（全文・原文ママ）

> 以下は発注者が提示した要件の全文。コピー（文言）はここから一字一句そのまま使うこと。

あなたは、BtoB企業のブランド戦略、Webディレクション、UI/UXデザイン、SEO、AI・DX導入支援、フルスタック開発に精通した専門家です。

既存サイト「CUCUL FM | 普段と新しい暮らしをサポートする会社」を、現在のブランド世界観と事業構造を維持しながら改善してください。

ククルFMは、以下の3領域を展開する会社です。

1. 建設・インフラ
- 建設業
- 調査・清掃
- 工具販売
- オリジナルクラフト販売

2. クリエイティブ・テック
- 映像制作・編集
- 資料作成
- AI業務支援
- Web開発
- メディア運営

3. ライフスタイル
- 犬関連事業
- オーナーズコモン等

既存サイトのブランド思想は、以下です。

- 普段と新しい暮らしをサポートする会社
- 建設・インフラ、クリエイティブ・テック、ライフスタイルの3つの柱に、テクノロジーを横串で通す
- 伝統的な産業に革新をもたらし、新しい価値を継続的に創造する
- お客様ごとのニーズに寄り添う
- 優れたツールや技術を、ただ導入するのではなく、その組織・現場に合う形へ消化・定着させる
- AIはAIらしく、人は人らしく働ける環境をつくる
- 「好きなことを形にする」を大切にする

上記を損なわず、ククルFMの新たな中核サービスとして、
「FDE・AI実装支援」を追加・強化してください。

FDEとは、Forward Deployed Engineerの略です。
顧客の現場や事業に深く入り込み、業務課題の整理、業務フロー設計、データ整備、AI・SaaS・業務システムの導入・実装、現場への定着、継続改善まで伴走する役割です。

ただし、サイト上では「FDE」という言葉を前面に出しすぎないでください。
FDEを知らない経営者・現場責任者にも伝わるように、
「現場伴走型のAI・DX実装支援」
「業務を理解し、使われる仕組みへ変える支援」
「導入から定着・改善まで伴走する技術支援」
といった平易な説明を優先してください。

重要な立ち位置は次の通りです。

- ククルFMは、単なるAIツール販売会社ではない
- 単なるWeb制作会社でもない
- 単なる受託開発会社でもない
- 単なるITコンサルティング会社でもない
- 現場や事業の実務を理解してから、最適な技術・運用・仕組みを設計し、実装し、定着まで伴走するチームである
- 建設・インフラを強みとして持つが、特定の業界だけに限定しない
- 現場業務と管理業務の連携に課題を持つ、幅広い事業者を対象とする
- 下水道、建設、設備、保守点検、清掃、物流、製造、不動産・施設管理、クリエイティブ事業、地域事業、サービス業などに展開可能な表現とする

---

# 最重要メッセージ

## サイト全体の補強キャッチコピー

現場と事業に、技術を定着させる。

## 補足コピー

AI・データ・クリエイティブを、
使われ続ける仕組みへ。

## FDE・AI実装支援ページのメインコピー

現場を知るから、変革を動かせる。

## FDE・AI実装支援ページのサブコピー

ククルFMは、業務の現場に入り込み、
課題整理から、AI・SaaS・データ基盤の設計と実装、
運用定着、継続改善まで伴走します。

ツールを導入するだけで終わらせず、
人が判断しやすく、チームが動きやすく、
日々の仕事が前に進む仕組みをつくります。

## ククルFMの思想として目立たせる文章

AIはAIらしく、人は人らしく。

AIにすべてを任せるのではなく、
情報整理、検索、記録、下書き、確認作業をAIが支援し、
人は判断、対話、創造、現場でしかできない仕事に集中する。

ククルFMは、技術を人の仕事に置き換えるためではなく、
人と事業の可能性を広げるために実装します。

---

# サイト改善方針

現在のトップページにある
「建設・インフラ」
「クリエイティブ・テック」
「ライフスタイル」
の3本柱は残してください。

FDE・AI実装支援は4本目の独立した事業柱として完全に切り離すのではなく、
3事業を横断する「TECHNOLOGY × FIELD」または
「FDE & AI IMPLEMENTATION」という横断セクションとして新設してください。

この横断セクションにより、
建設・インフラの現場知識、
クリエイティブ・テックのAI・Web・映像・資料作成能力、
ライフスタイル領域で培う顧客に寄り添う視点を統合していることを伝えてください。

---

# トップページ構成

## 1. ヘッダー

ロゴ：
CUCUL FM .LLC

ナビゲーション：
- Our Service
- FDE・AI実装支援
- Construction & Infrastructure
- Creative & Tech
- Lifestyle
- Insights
- About Us
- Contact

右上の強調CTA：
「相談する」

モバイルではハンバーガーメニューとし、
「相談する」ボタンは常に視認しやすい位置に配置する。

## 2. ファーストビュー

既存のメインメッセージ：
普段と新しい暮らしをサポートする会社

この世界観は残す。

その下またはアニメーション切り替え要素で、以下を表示する。

現場と事業に、技術を定着させる。

建設・インフラ、クリエイティブ・テック、ライフスタイル。
異なる事業を横断し、AI・データ・クリエイティブの力で、
人と現場がより良く働く仕組みをつくります。

CTA：
- ククルFMの事業を見る
- FDE・AI実装支援を見る

ヒーロービジュアルは、特定の下水道・建設現場だけに限定しない。
以下を抽象的かつ上質に統合したビジュアルにする。

- 現場で働く人
- タブレットやスマートフォン
- データの流れ
- ドキュメントや台帳
- 建設・インフラ
- 映像・クリエイティブ
- 犬や暮らしなどのライフスタイル
- 人、技術、地域、日常のつながり

安易なロボット画像、ネオン調、サイバーパンク調、安っぽいAI生成風の人物画像は使わない。

## 3. FDE横断セクション

セクション名：
FDE & AI IMPLEMENTATION

日本語見出し：
技術を、現場で使われる仕組みへ。

本文：
良いツールも、現場の仕事に合わなければ使われ続けません。

ククルFMは、業務の流れ、利用する人、既存の情報、
組織ごとの判断やルールを理解したうえで、
AI・SaaS・Webシステム・データベース・映像・資料を組み合わせます。

構想だけで終わらせず、
導入、教育、運用、改善まで。
事業と現場に、技術を定着させます。

3ステップ図解：

01 Understand
現場と事業を理解する

現場、事務、管理、経営の業務フローを整理し、
情報の分断、属人化、転記、確認待ち、手戻りなどの課題を見つけます。

02 Build
仕組みを設計・実装する

既製SaaS、AI、Web、業務システム、データベース、
地図・位置情報、映像、帳票設計を組み合わせ、
本当に必要な形へ実装します。

03 Embed
使われ続ける状態へ育てる

試験運用、操作説明、運用ルール、利用状況の確認、
継続改善を通じて、仕組みを現場と組織に定着させます。

CTA：
「FDE・AI実装支援の詳細を見る」

## 4. サービスセクション

現在の3事業領域を維持する。

各領域に「FDE・AI実装支援とのつながり」が分かる補足を追加する。

### CONSTRUCTION & INFRASTRUCTURE

見出し：
建設・インフラ

本文：
社会基盤を支える建設施工、調査・清掃、工具・クラフト。
実務で培った知識を生かし、安全性、品質、効率を支えるサービスを提供します。

補足：
現場記録、写真・動画、点検・調査、台帳、報告、進捗管理など、
現場に根ざしたDX・AI実装も支援します。

### CREATIVE & TECH

見出し：
クリエイティブ・テック

本文：
映像、資料、Web、AI、メディア。
伝える力と技術を掛け合わせ、事業の課題を解決し、
新しい価値の届け方を設計します。

補足：
業務フローの設計、AI活用、情報整理、Webシステム、
データベース、社内外への情報発信まで、一貫した仕組みづくりを支援します。

### LIFESTYLE

見出し：
ライフスタイル

本文：
犬に関わる事業をはじめ、
「好きなことを形にする」体験とサービスを提供します。
暮らしに寄り添い、日常に新しい価値と彩りを加えます。

補足：
顧客との接点、予約・記録・コミュニティ・情報発信など、
サービス事業の運営を支える仕組みづくりにも取り組みます。

## 5. 課題提起セクション

見出し：
こんな課題はありませんか。

以下を、業界を限定しない表現のカードとして表示する。

- 現場の情報が紙、Excel、写真、チャット、メール、PDFに分散している
- 必要な資料や過去の記録を探すだけで時間がかかる
- 現場と事務所、担当者と管理者の間で、同じ情報を何度も入力・確認している
- 仕事の進め方や判断基準が、特定の経験者に依存している
- システムを入れたが、現場では使われず、結局元の運用に戻っている
- AIを使いたいが、どこから始めればよいか分からない
- 業務は忙しいため、大規模なシステム導入は現実的ではない
- 事業の成長に合わせ、情報管理と仕事の進め方を整えたい

セクション末尾：
ククルFMは、ツールありきでは考えません。
今ある仕事、情報、人の動きを理解し、
無理なく続けられる仕組みから一緒につくります。

## 6. 12か月ロードマップ

このセクションは、FDE支援の重要コンテンツとして、
タイムラインまたは4フェーズのカードで、非常に見やすく実装する。

見出し：
12か月で、現場の課題を使われる仕組みへ。

導入文：
最初から完璧な仕組みを目指すのではなく、
優先度の高い業務から始めます。
現場で試し、改善し、データと運用を育てながら、
継続的に成果を生む業務基盤へつなげます。

### 1〜3か月：現場課題を、実装できる要件へ

- 現場、事務、管理、経営の業務フローを可視化する
- 紙、Excel、メール、チャット、写真、PDF、既存ツールなどの情報分断を整理する
- 二重入力、転記、確認待ち、手戻り、属人化などの課題を特定する
- 業務時間、対応日数、差戻し、入力漏れなど、改善対象のKPIを3つ程度に絞る
- 最初に対象とする1つの業務、帳票、部門、拠点、顧客対応プロセスを選定する

成果物：
- 現状業務フロー
- 課題と優先順位の整理
- データ一覧
- 要件定義のたたき台
- KPI設計
- 導入ロードマップ

### 4〜6か月：最小構成を作り、実務で動かす

- 対象業務に必要な案件管理、記録、書類、写真、承認、検索、帳票などの最小機能を設計する
- 役割ごとに、入力項目、画面、権限、承認フローを整える
- Excel、CSV、クラウドストレージ、既存SaaS、既存システムとの連携方法を検討する
- 1つの現場、部門、業務工程、または顧客対応で試験運用する
- AIは、資料検索、情報整理、議事録・記録の要約、帳票の下書きなど、明確な1用途から始める
- AIの出力は、必ず人が確認・承認する運用とする

成果物：
- 最小実用プロダクト
- 初期データ・マスタ
- 操作マニュアル
- 試験運用レポート
- 改善項目リスト

### 7〜9か月：現場に定着させ、利用範囲を広げる

- 現場担当者、事務担当者、管理者向けに、操作説明と運用ルールを整備する
- 利用時の負担、入力漏れ、例外処理、画面の使いやすさを改善する
- 別の現場、部門、拠点、協力会社、サービスへ段階的に展開する
- 個別の要望と、組織全体で標準化すべき機能・運用を分けて整理する
- 利用率、入力率、問い合わせ、データ品質を確認する
- 導入効果をKPIで中間評価する

成果物：
- 運用ルール
- 教育資料
- 権限・承認設計
- 改善済み機能
- 中間効果レポート
- 横展開計画

### 10〜12か月：成果を可視化し、継続改善できる体制へ

- 導入前後の業務時間、対応・提出日数、差戻し、再入力、確認工数などを比較する
- 現場、管理者、経営者の視点で、継続すべき運用と改善点を整理する
- データ活用の範囲を、進捗、品質、原価、顧客対応、営業、保全、事業判断へ広げる
- 次年度のDX・AI活用ロードマップと投資優先順位を策定する
- 担当者個人に依存しない、社内の運用体制を整える

成果物：
- 導入成果レポート
- KPI評価
- 改善バックログ
- 次年度DX・AI活用計画
- 社内運用体制案
- 継続支援プラン

CTA：
「自社に合わせた進め方を相談する」

## 7. 活用テーマ

見出し：
現場と事業の、こんな仕事から始められます。

以下は実績ではなく、「活用イメージ」と明記する。

- 現場記録、作業報告、点検情報、写真、動画、書類を案件・顧客単位で整理する
- Excelや紙で管理している台帳を、検索・共有・履歴確認・集計しやすい形に整える
- 現場や外出先から、スマートフォン・タブレットで記録・報告できるようにする
- 報告書、議事録、提案書、点検記録などの下書きをAIで支援する
- 過去の資料、案件履歴、ノウハウ、マニュアルを横断検索できるようにする
- 問い合わせ、案件、作業、顧客、担当者、書類をつなぎ、対応漏れを減らす
- 業務の進捗や品質を、管理者がリアルタイムに把握しやすくする
- 映像、写真、資料、Webを活用し、社内教育、営業、採用、顧客説明を改善する
- 予約、顧客管理、サービス記録、情報発信などを整え、サービス運営を改善する
- 地図・位置情報と現場・設備・顧客情報を結び付け、運用や計画に活用する

## 8. FAQ

Q. FDEとは何ですか？
A. FDEは、現場や事業の課題を理解したうえで、AI・SaaS・データ・システムを活用する仕組みを設計・実装し、導入後の定着・改善まで伴走する役割です。ククルFMでは、分かりやすく「現場伴走型のAI・DX実装支援」と表現しています。

Q. 建設・インフラ以外の業種でも相談できますか？
A. はい。現場業務と事務・管理業務の連携、情報の分散、属人化、帳票作成、顧客対応、データ活用などに課題がある事業であれば、業種を問わずご相談いただけます。

Q. ITに詳しい社員がいなくても大丈夫ですか？
A. はい。まずは現在の業務の進め方と、紙・Excel・写真・書類・既存ツールなどの状態を確認します。専門用語に偏らず、優先順位と最初の一歩を整理します。

Q. 今使っているExcelやシステムを、すべて置き換える必要がありますか？
A. いいえ。既存の運用やデータを活かし、負荷と効果の大きい部分から段階的に改善します。必要に応じて既存SaaSやExcel、CSVなどとの連携も検討します。

Q. AIに業務の判断を任せることになりますか？
A. いいえ。AIは、検索、整理、要約、下書きなどを補助する用途から活用します。品質・安全・顧客対応に関わる最終判断は、人が確認・承認する運用を前提に設計します。

Q. 小さなテーマから始められますか？
A. はい。1つの帳票、1つの現場、1つの部門、1つの業務プロセスなど、効果を検証しやすい範囲から始めることを推奨します。

Q. 相談段階でも問い合わせできますか？
A. はい。「何をデジタル化すべきか分からない」「AIを使える業務があるか知りたい」といった構想段階からご相談いただけます。

## 9. お問い合わせ

見出し：
困りごとが整理できていなくても、ご相談ください。

本文：
「現場と事務所の連携をもっと良くしたい」
「情報を探す時間を減らしたい」
「AIを使いたいが、何から始めればよいか分からない」
「今の仕事に合うシステムを考えたい」
「新しい事業やサービスを形にしたい」

ククルFMは、課題の整理から伴走します。
現在の仕事の流れと、気になっていることをお聞かせください。

CTA：
「相談する」

フォーム項目：
- 会社名または屋号
- お名前
- 部署・役職
- メールアドレス
- 電話番号
- 業種
- ご相談の種類
  - 現場業務の改善
  - AI活用
  - Web・業務システム
  - データ・情報整理
  - 資料・映像制作
  - 新規事業・サービス設計
  - その他
- ご相談内容
- 個人情報保護方針への同意

---

# デザイン・実装上の要件

- 既存のCUCUL FMサイトのミニマルで洗練された世界観を維持する。
- 英語見出しと日本語本文を組み合わせる既存のトーンを踏襲する。
- FDE関連は、硬くて難しいIT用語だけにならないようにする。
- 全ページを日本語中心とする。
- モバイル、タブレット、PCで読みやすく実装する。
- FDE・AI実装支援の詳細ページを新設する。
- トップページにはFDE横断セクションへの導線を置く。
- 既存の「AI業務支援」「Web開発」を、FDE・AI実装支援の思想と自然につなげる。
- サービスカード、FAQ、ロードマップ、活用テーマはデータ配列化し、将来の追加・編集を容易にする。
- 仮の実績数値、架空の導入社名、架空の顧客ロゴ、根拠のない削減効果は作らない。
- 「必ず効率化できる」「完全自動化」「100%正確なAI」などの断定表現は使用しない。
- SEO title、meta description、OGP、構造化データ、FAQ schemaを設定する。
- お問い合わせフォームは、必須チェック、メール形式チェック、送信中状態、成功・失敗状態、スパム対策を実装する。
- 会社名、所在地、代表者名、電話番号、メールアドレス、SNS、実績情報は設定ファイルに集約し、後から更新可能にする。
- Next.js、TypeScript、Tailwind CSSで実装する場合、再利用可能で保守しやすいコンポーネント設計にする。

最終的に出力するもの：

1. 改修後のサイトマップ
2. トップページの詳細ワイヤーフレーム
3. FDE・AI実装支援ページの詳細ワイヤーフレーム
4. ページごとの完成した日本語コピー
5. SEO title・meta description・OGP案
6. デザインシステム
7. 実装可能なフロントエンドコード
8. お問い合わせフォーム
9. 実績・会社情報を更新する箇所の説明
10. 公開前チェックリスト

---

# SEO・AIO・GEO・LLMO 実装要件

このWebサイトは、従来の検索エンジン最適化だけでなく、
Google AI Overviews、Google AI Mode、Microsoft Copilot、
BingのAI回答、Perplexity、ChatGPT、Claudeなどの生成AIが、
ククルFMの事業内容、専門性、提供範囲を正しく理解・参照しやすい状態を目指す。

ただし、AI検索での表示・引用・上位表示を保証する表現は一切使用しない。
AI向けの隠しテキスト、検索順位を不正に操作する施策、内容と一致しない構造化データ、
架空の実績・顧客・数値・レビュー、キーワードの過剰な反復は実装しない。

重要なのは、検索エンジン、生成AI、顧客のすべてが、
ククルFMを正確に理解できる、一次性・専門性・信頼性の高い情報設計である。

## 1. エンティティ情報の統一

ククルFMについて、サイト全体で以下の情報を表記ゆれなく統一する。

正式名称：
CUCUL FM .LLC
または
ククルFM合同会社

ブランド名：
CUCUL FM
ククルFM

事業の定義：
ククルFMは、建設・インフラ、クリエイティブ・テック、
ライフスタイルの領域で事業を展開する会社である。

FDE事業の定義：
ククルFMは、現場伴走型のFDE・AI実装支援を提供する。
顧客の業務と現場を理解し、業務フロー整理、データ整備、
SaaS・AI・Webシステムの設計・実装、運用定着、継続改善まで支援する。

サイト上の表記は、会社概要、トップページ、FDEページ、FAQ、
お問い合わせページ、フッター、構造化データで一貫させる。

会社名、ロゴURL、代表者、所在地、電話番号、メールアドレス、
法人番号、創業・設立年月日、SNS、営業時間、問い合わせ先は、
設定ファイルから一元管理できる構成にする。

情報が未確定の場合には、推測で埋めず、
管理画面または設定ファイルで差し替えるプレースホルダーにする。

## 2. コンテンツ設計

各ページは、1ページにつき1つの主要テーマを持つ。
1ページ内で複数テーマを詰め込みすぎず、
URL、title、H1、導入文、H2構造、FAQを同じ検索意図に揃える。

すべての重要ページは、以下の構成を原則とする。

1. H1直下に、ページの結論・定義・提供価値を1〜3文で明記する
2. 対象者、解決する課題、提供内容を明確にする
3. 実際の進め方、判断基準、注意点を説明する
4. ククルFM独自の視点、現場経験、方法論、図解、テンプレート、事例を加える
5. よくある質問を設置する
6. 次に取るべき行動をCTAとして明示する
7. 執筆者または監修者、公開日、最終更新日を表示する

重要な結論や定義は、JavaScript実行後でなければ表示されない形にしない。
サーバーサイドレンダリングまたは静的生成を利用し、
HTML内に重要なテキストコンテンツを出力する。

## 3. 優先して作成するコンテンツページ

以下のページを、FDEサービスページとは別に作成する。
すべて、一般論の再編集ではなく、ククルFMが現場で持つ視点・判断基準・実務経験を反映する。

- /fde
  ページ名：FDE・AI実装支援
  テーマ：現場伴走型FDEとは何か、ククルFMが提供する支援範囲

- /insights/fde-toha
  ページ名：FDEとは何か
  テーマ：FDEの定義、役割、ITコンサル・SES・受託開発との違い

- /insights/genba-dx
  ページ名：現場DXとは何か
  テーマ：現場DXの定義、失敗しやすい理由、進め方、成功の判断基準

- /insights/ai-teichaku
  ページ名：AI導入が現場に定着しない理由
  テーマ：導入失敗の要因、業務設計、教育、ルール、評価の重要性

- /insights/gyomu-flow
  ページ名：業務フロー診断の進め方
  テーマ：現場・事務・管理者の業務を可視化し、改善テーマを決める方法

- /insights/sas-vs-custom
  ページ名：SaaS導入と個別開発の選び方
  テーマ：既製SaaS、ノーコード、個別開発、既存システム連携の判断軸

- /insights/ai-usecases
  ページ名：現場・バックオフィスにおけるAI活用例
  テーマ：情報検索、記録の要約、帳票下書き、写真・書類整理、顧客対応補助と、人が最終判断すべき範囲

- /insights/data-foundation
  ページ名：散らばる情報を業務データへ変える方法
  テーマ：紙、Excel、CSV、写真、PDF、メール、チャット、既存システムの情報整理

- /insights/dx-roadmap
  ページ名：中小企業がDXを進める12か月ロードマップ
  テーマ：課題整理、最小実用プロダクト、試験運用、定着、効果測定、横展開

各記事には、関連するサービスページ、問い合わせページ、
関連記事への内部リンクを適切に配置する。

## 4. 構造化データ

JSON-LD形式で、実際の可視コンテンツと完全に一致する構造化データを実装する。

必須候補：
- Organization
- WebSite
- WebPage
- BreadcrumbList
- Service
- FAQPage
- Article
- ProfilePage
- LocalBusiness
  ただし、実店舗・訪問先・所在地・サービス提供地域などの条件が適切な場合のみ使用する

Organizationには、設定済みの正確な情報のみを含める。
会社名、alternateName、url、logo、description、sameAs、
contactPoint、foundingDate、addressなどは、裏付けがあるものだけを設定する。

Serviceには、以下のサービスを設定する。

- FDE・AI実装支援
- 業務フロー・DX診断
- AI業務活用支援
- 業務システム・Web開発
- データ整理・データ連携支援
- 映像・資料制作
- 建設・インフラ関連事業
- ライフスタイル関連事業

FAQPageは、画面上に実際に表示されている質問と回答だけをマークアップする。
リッチリザルトやAI表示だけを目的とした、非表示のFAQを作らない。

Articleには、headline、description、datePublished、dateModified、
author、image、mainEntityOfPageを正確に設定する。

## 5. 技術SEO

以下を実装・確認する。

- robots.txtを設置する
- sitemap.xmlを自動生成する
- canonical URLを全ページに設定する
- noindex設定は、開発環境、重複ページ、不要な検索結果ページだけに限定する
- 重要ページがrobots.txt、meta robots、X-Robots-Tag、CDN設定でブロックされていないことを確認する
- すべての重要ページがHTTP 200で返ることを確認する
- 404ページを設置する
- URLは意味のある英小文字・ハイフン区切りとする
- クライアントサイドだけで本文を描画しない
- Core Web Vitalsを意識する
- 画像はWebPまたはAVIFを基本とし、幅・高さを指定してCLSを防ぐ
- 画像には内容を説明するalt属性を付ける
- Lazy loadingを適切に利用する
- フォント、JavaScript、動画、画像を最適化する
- 見出し構造はH1を各ページ1つとし、H2・H3を論理的に使用する
- パンくずリストを表示・構造化する
- すべての重要ページを、グローバルナビゲーションまたは関連ページから内部リンクする
- 検索結果に出したくないテストページやプレビューURLを作らない
- Open Graph、Twitter Card、favicon、Web App Manifestを実装する

## 6. ローカル・信頼性情報

会社概要ページを充実させる。

掲載候補：
- 会社名
- 代表者
- 所在地
- 連絡先
- 事業内容
- 対応地域
- 設立年月日
- ミッション
- 会社として大切にする考え方
- 建設・インフラ、クリエイティブ・テック、ライフスタイルの関係
- FDE・AI実装支援の体制
- 問い合わせ方法
- プライバシーポリシー
- 特定商取引法に基づく表記
  ECまたは対象サービスがある場合のみ
- 利用規約
- 情報セキュリティ・AI利用方針
  必要に応じて作成する

FDE・AI活用に関するページでは、
AIに入力してよい情報・扱うべきでない情報、
人による確認・承認の考え方、
顧客データの取扱い、
個別案件におけるセキュリティ確認の姿勢を明記する。

## 7. 計測設計

Google Tag Managerを導入できる構成にする。
Google Analytics 4とGoogle Search Consoleを設定可能にする。

以下のイベントを設計する。

- contact_form_view
- contact_form_start
- contact_form_submit
- contact_form_success
- click_phone
- click_email
- click_consultation_cta
- click_fde_service
- click_roadmap
- download_document
  資料ダウンロードを実装する場合のみ
- view_case_study
  事例ページを実装する場合のみ
- scroll_depth_50
- scroll_depth_90

イベント名と実装箇所をドキュメント化する。

Microsoft Bing Webmaster Toolsのサイト登録と、
AI Performance機能を確認できる導線・運用手順も、
公開後チェックリストに追加する。

## 8. 更新・運用ルール

サイト公開後は、以下を定期的に確認する運用を前提とする。

毎月：
- Google Search Consoleで検索クエリ、表示回数、クリック、インデックス状況を確認する
- GA4で問い合わせ、CTAクリック、フォーム離脱、流入ページを確認する
- Bing Webmaster Toolsで検索状況およびAI Performanceのデータを確認する
- 問い合わせで実際に使われた言葉を、FAQ・記事・サービス説明に反映する
- 古くなった記事やサービス説明を更新する

四半期ごと：
- 新しい業界別ページ、ユースケース、一次情報の記事を追加する
- 実際の支援事例を、顧客承諾・守秘義務・匿名化方針に従って公開する
- 導入前後の状況、支援範囲、判断、成果、制約を具体的に記録する
- 会社情報、サービス内容、対応領域、連絡先、構造化データの整合性を監査する

## 9. 公開前チェックリスト

以下を、管理者が確認できるMarkdown形式のチェックリストとして出力する。

- [ ] 各ページに明確なH1、title、meta descriptionがある
- [ ] 各ページの内容と構造化データが一致している
- [ ] 会社名・サービス名・説明文に表記ゆれがない
- [ ] 重要な本文がHTMLとして取得できる
- [ ] robots.txt、sitemap.xml、canonical、noindex設定を確認した
- [ ] モバイルでCTAとフォームが正常に機能する
- [ ] フォーム送信後の計測イベントが発火する
- [ ] 404ページとリダイレクト方針を確認した
- [ ] OGP画像とSNS共有時の表示を確認した
- [ ] Google Search Consoleに登録した
- [ ] Google Analytics 4を設定した
- [ ] Bing Webmaster Toolsに登録した
- [ ] Google Business Profileの登録・情報整合性を確認した
- [ ] 会社概要、プライバシーポリシー、問い合わせ先を掲載した
- [ ] AIに関する表現に、過度な性能保証や誤認表現がない
- [ ] 架空の事例、実績、顧客ロゴ、数値を掲載していない
- [ ] 実在する一次情報、写真、図解、判断基準、運用ノウハウを掲載している

（要件定義ここまで）
